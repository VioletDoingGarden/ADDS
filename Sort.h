#ifndef SORT_H
#define SORT_H
#include <vector>

class Sort {
private:
    std::vector<int> list;
public:
    std::vector<int> sort(std::vector<int> list) {
        return list;
    };
};
#endif // SORT_H