#ifndef SORT_H
#define SORT_H
#include <vector>
#include <utility>

class Sort {
public:
    std::vector<int> list;
    std::vector<int> sort(std::vector<int> list) {
        return list;
    };
};
#endif // SORT_H