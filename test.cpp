#include <iostream>
#include <cmath>

int main() {
    int n = 100; // example value
    int limit = std::sqrt(n);
    for (int i = 1; i <= limit; i++) {
        std::cout << 1 << std::endl;
    }
    return 0;
}